import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ["anthony-giant-washington-slightly.trycloudflare.com"],
};

export default nextConfig;
